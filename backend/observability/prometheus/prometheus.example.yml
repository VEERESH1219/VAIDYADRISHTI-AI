global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts/alerts.example.yml

scrape_configs:
  - job_name: "vaidyadrishti-api"
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - api:3001

